## Summary

<!-- 1–3 bullets: what changed and why. The "what" is in the diff; lead with the "why". -->

-

## Test plan

- [ ] `mvn verify` passes locally
- [ ] Live tests pass against the real Fanar API (if applicable, with `FANAR_API_KEY` set)

<!--
The detailed engineering checklist (no version ranges, no split packages, deprecated API hygiene,
core-vs-adapter constraints, …) lives in `docs/JAVA_LIBRARY_BEST_PRACTICES.md`. CI enforces every
machine-checkable item from there on every PR — there's no point copying it into the template.
-->
