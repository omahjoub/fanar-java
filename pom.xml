<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>qa.fanar</groupId>
    <artifactId>fanar-java</artifactId>
    <version>0.7.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Fanar Java SDK</name>
    <description>Java SDK for Qatar's Fanar AI platform — typed, pluggable, observable
        transport over the Fanar API plus Spring Boot 4 / Spring AI 2.0 framework adapters.
    </description>
    <url>https://github.com/omahjoub/fanar-java</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Oussama Mahjoub</name>
            <email>mahjoub.oussama@gmail.com</email>
            <url>https://github.com/omahjoub</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/omahjoub/fanar-java</url>
        <connection>scm:git:https://github.com/omahjoub/fanar-java.git</connection>
        <developerConnection>scm:git:git@github.com:omahjoub/fanar-java.git</developerConnection>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/omahjoub/fanar-java/issues</url>
    </issueManagement>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Version catalog — pin every dependency we use explicitly. Library first, not application. -->
        <junit-bom.version>6.1.3</junit-bom.version>
        <jackson3-bom.version>3.2.2</jackson3-bom.version>
        <jackson2-bom.version>2.22.2</jackson2-bom.version>
        <slf4j.version>2.0.19</slf4j.version>
        <opentelemetry.version>1.65.0</opentelemetry.version>
        <micrometer.version>1.17.1</micrometer.version>
        <spring-boot.version>4.1.1</spring-boot.version>
        <spring-ai.version>2.0.1</spring-ai.version>
        <!-- Google ADK line the fanar-adk adapter is measured against (ADR-030); genai, RxJava and
             reactive-streams are what that ADK release declares, pinned so a Boot bump cannot move them. -->
        <google-adk.version>1.9.0</google-adk.version>
        <google-genai.version>1.58.0</google-genai.version>
        <rxjava.version>3.1.12</rxjava.version>
        <reactive-streams.version>1.0.4</reactive-streams.version>
        <maven-surefire-plugin.version>3.6.0</maven-surefire-plugin.version>
        <maven-compiler-plugin.version>3.16.0</maven-compiler-plugin.version>
        <maven-dependency-plugin.version>3.11.0</maven-dependency-plugin.version>
        <maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>

        <!--
          Reproducible builds: fixes every archive entry's timestamp so the same commit produces
          byte-identical artifacts. Independent of where we publish, and a prerequisite if we ever
          publish to Maven Central. Bump to the release date when cutting a release; `artifact:compare`
          verifies the result.
        -->
        <project.build.outputTimestamp>2026-09-16T16:25:38Z</project.build.outputTimestamp>

        <!-- Test -->
        <jacoco.version>0.8.15</jacoco.version>
        <assertj.version>3.27.7</assertj.version>
        <jacoco.line.coverage>1.00</jacoco.line.coverage>
        <jacoco.branch.coverage>1.00</jacoco.branch.coverage>
        <jacoco.instruction.coverage>1.00</jacoco.instruction.coverage>
        <jacoco.method.coverage>1.00</jacoco.method.coverage>
        <jacoco.complexity.coverage>1.00</jacoco.complexity.coverage>
    </properties>

    <modules>
        <module>core</module>
        <module>json-jackson2</module>
        <module>json-jackson3</module>
        <module>obs-slf4j</module>
        <module>obs-otel</module>
        <module>obs-micrometer</module>
        <module>interceptor-logging</module>
        <module>spring-boot-4-starter</module>
        <module>spring-boot-4-sample</module>
        <module>spring-ai-starter</module>
        <module>spring-ai-sample</module>
        <module>adk</module>
        <module>bom</module>
        <!--
          `test-support` is the JDK-only fixture module behind every `*IntegrationTest` (scripted
          loopback HttpServer, collecting Flow.Subscriber). Never published (`maven.deploy.skip`),
          no coverage gate; it has no dependency on core so core's tests can use it.
        -->
        <module>test-support</module>
        <!--
          `e2e` is part of the reactor so IDEs auto-detect it. It adds ~1 second to
          `./mvnw verify`. Its pom.xml disables publication (`maven.deploy.skip=true`) and
          every quality gate core enforces, and the live integration tests inside it are
          annotated `@EnabledIfEnvironmentVariable("FANAR_API_KEY")` so they skip silently
          on any machine without a key — including PR CI.
        -->
        <module>e2e</module>
        <module>e2e-graalvm</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>tools.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson3-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson2-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
              <groupId>org.slf4j</groupId>
              <artifactId>slf4j-bom</artifactId>
              <version>${slf4j.version}</version>
              <type>pom</type>
              <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>${opentelemetry.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-bom</artifactId>
                <version>${micrometer.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Pinned here rather than inherited from spring-boot-dependencies, so a Boot bump
                 cannot move the assertion library under the Spring modules' tests unnoticed. -->
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
            </dependency>
            <!-- The Google ADK adapter's provided-scope stack (ADR-030). Plain pins: ADK publishes no BOM,
                 and RxJava / reactive-streams are also managed by spring-boot-dependencies, so an explicit
                 entry keeps the adapter on the versions its ADK release declares. -->
            <dependency>
                <groupId>com.google.adk</groupId>
                <artifactId>google-adk</artifactId>
                <version>${google-adk.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.genai</groupId>
                <artifactId>google-genai</artifactId>
                <version>${google-genai.version}</version>
            </dependency>
            <dependency>
                <groupId>io.reactivex.rxjava3</groupId>
                <artifactId>rxjava</artifactId>
                <version>${rxjava.version}</version>
            </dependency>
            <dependency>
                <groupId>org.reactivestreams</groupId>
                <artifactId>reactive-streams</artifactId>
                <version>${reactive-streams.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>


    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <!-- Emit method parameter names into bytecode. Spring binds
                             `@PathVariable String id` reflectively by name, and without this flag it
                             fails at runtime rather than at compile time. Our own code always names
                             the binding explicitly, so nothing here depends on it today — the flag
                             is what keeps that a style choice instead of a requirement, for us and
                             for anyone building on the starters. -->
                        <parameters>true</parameters>
                        <!-- Javadoc quality gate. Doclint runs at javac time;
                             failures break the build. -missing is kept off by default until the SDK
                             has a stable public API; tighten to 'all' once 1.0 approaches. -->
                        <compilerArgs>
                            <arg>-Xdoclint:all,-missing</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                </plugin>
                <plugin>
                  <groupId>org.apache.maven.plugins</groupId>
                  <artifactId>maven-jar-plugin</artifactId>
                  <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <!--
                      A -sources.jar and a -javadoc.jar per published module. Maven Central requires
                      both; every other distribution channel is merely better for having them, and
                      the javadoc run is a stronger check than javac's doclint because it resolves
                      cross-module @link targets and actually renders the HTML.
                      Bound in the `publish` profile below so a normal `verify` stays fast.
                    -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <notimestamp>true</notimestamp>
                        <quiet>true</quiet>
                        <doclint>all,-missing</doclint>
                        <!--
                          No automatic cross-linking to reactor siblings. Enabled (the default) it
                          does two things, and both are wrong here:

                          1. It emits hyperlinks derived from the project <url>, e.g.
                             https://github.com/omahjoub/fanar-java/fanar-core/apidocs/... — a URL
                             where no site is published, so every cross-module reference in the
                             javadoc jar 404s. A dead link is worse than plain text: it looks like a
                             working reference.
                          2. It tries to read package-lists from siblings that skip javadoc
                             (test-support), logging one ignored [ERROR] per dependent module —
                             noise that would hide a real javadoc failure in a release log.

                          Turning it off loses nothing real: the type names still appear, just
                          unlinked. The proper fix is an explicit <links> entry pointing at the
                          published javadoc (javadoc.io, say), which needs a published coordinate —
                          blocked on the publication decision in docs/PROJECT_STATE.md.
                        -->
                        <detectOfflineLinks>false</detectOfflineLinks>
                    </configuration>
                </plugin>
                <plugin>
                    <!-- Surefire 3.x is required for JUnit Platform; pin explicitly so we don't
                         inherit an older default from Maven's built-in super-POM. -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <!-- jdk.httpserver backs the scripted local server every *IntegrationTest
                             runs against (fanar-java-test-support). The JPMS modules declare it
                             `requires static` at most, so it must be added to the test module graph
                             explicitly; the @{argLine} prefix preserves JaCoCo's javaagent. -->
                        <argLine>@{argLine} --add-modules=jdk.httpserver</argLine>
                        <properties>
                            <!-- Hang backstop, not a performance budget: a stuck latch or
                                 Flow.Publisher fails the test with a stack trace instead of hanging
                                 the fork. Interrupts the test thread (SAME_THREAD mode). Off while a
                                 debugger is attached. e2e raises the default for live calls. -->
                            <configurationParameters>
                                junit.jupiter.execution.timeout.default = 60 s
                                junit.jupiter.execution.timeout.mode = disabled_on_debug
                            </configurationParameters>
                        </properties>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <!-- Fail the build on undeclared or unused direct dependencies.
                     Skipped automatically on pom-packaging projects; kicks in for every JAR module. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>analyze</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>analyze-only</goal>
                        </goals>
                        <configuration>
                            <failOnWarning>true</failOnWarning>
                            <ignoredUnusedDeclaredDependencies>
                                <!-- JUnit Platform engine: needed on the test classpath for test
                                     execution, but never imported by user code. Standard whitelist. -->
                                <ignoredUnusedDeclaredDependency>org.junit.jupiter:junit-jupiter-engine:*</ignoredUnusedDeclaredDependency>
                                <!-- SLF4J no-op binding: runtime-only test dependency that
                                     suppresses SLF4J's "no provider found" warning when an
                                     `obs-*` module's public no-arg constructor is exercised.
                                     Has no compile-time imports. -->
                                <ignoredUnusedDeclaredDependency>org.slf4j:slf4j-nop:*</ignoredUnusedDeclaredDependency>
                                <!-- The *IntegrationTest classes of the core-only modules decode real
                                     wire JSON through the Jackson 3 codec, which FanarClient discovers via
                                     ServiceLoader (ADR-008) — never imported. The codec module declares
                                     Jackson `provided`, so its runtime must be on the test module path
                                     explicitly. No compile-time imports for either. -->
                                <ignoredUnusedDeclaredDependency>qa.fanar:fanar-json-jackson3:*</ignoredUnusedDeclaredDependency>
                                <ignoredUnusedDeclaredDependency>tools.jackson.core:jackson-databind:*</ignoredUnusedDeclaredDependency>
                            </ignoredUnusedDeclaredDependencies>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.instruction.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.branch.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>METHOD</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.method.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>COMPLEXITY</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.complexity.coverage}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
          `-Ppublish` adds the -sources.jar and -javadoc.jar that a published artifact needs.
          Kept out of the default build so `./mvnw verify` stays fast: a full javadoc run is
          slower than everything else in this reactor combined, and it proves nothing a
          contributor needs proved on every commit. Enabled manually for now: the GitHub-Release
          flow does not ship these jars, and wiring it into `release.yml` waits on the publication
          target (ADR-029).

          Modules that are never published (samples, e2e, e2e-graalvm, test-support) opt out with
          maven.source.skip / maven.javadoc.skip next to their existing maven.deploy.skip.
        -->
        <profile>
            <id>publish</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals><goal>jar-no-fork</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals><goal>jar</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
