# SLF4J Simple binding configuration for the e2e module's live tests.
#
# Enables DEBUG-level output for the SDK's observability loggers (`fanar.*`) so a developer
# running a live test sees one structured line per SDK operation alongside the existing
# wire-bytes output produced by `LoggingInterceptor.toStdOut()`. Other libraries stay at INFO.

org.slf4j.simpleLogger.defaultLogLevel=info
org.slf4j.simpleLogger.log.fanar=debug

org.slf4j.simpleLogger.showDateTime=true
org.slf4j.simpleLogger.dateTimeFormat=HH:mm:ss.SSS
org.slf4j.simpleLogger.showLogName=true
org.slf4j.simpleLogger.showShortLogName=false
org.slf4j.simpleLogger.showThreadName=false
org.slf4j.simpleLogger.levelInBrackets=true
